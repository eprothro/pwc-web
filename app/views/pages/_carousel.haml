- slides ||= []
#myCarousel.carousel.slide
  .carousel-inner
    = link_to about_path, class: 'item partners active' do
      .title
        PW Partners
      .names.visible-desktop
        .row-fluid
          .span6.bob Bob Roseman, CPA
          .span6.tom Tom Prothro, CPA
        .row-fluid
          .span6.kristi Kristi Moore, CPA
          .span6.walter Walter Wilhelmi, CPA
      .image
        = image_tag('slideshow/partners_300x376.jpg')

    - employees.each_with_index do |e, i|
      = link_to employee_path(e), class: 'item employee' do
        = image_tag(e.image_url)
        .name
          = e.name

