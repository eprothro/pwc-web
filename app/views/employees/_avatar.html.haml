- role ||= @role || employee.subtitle
.avatar
  = link_to employee do
    .image
      = image_tag(employee.image_url)
      .name{class: ('subtitle' if role.present?)}
        .title= employee.image_title
        - if role.present?
          .subtitle= role